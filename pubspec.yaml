name: recordatorio_pastilla
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  awesome_notifications: ^0.10.1
  animate_do: ^3.3.9
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Movidos aquí para que el generador de código funcione:
  hive_generator: ^2.0.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true